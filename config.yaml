# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: aragon-indexer
description: Aragon DAO Protocol Indexer

field_selection:
  transaction_fields:
    - hash
    - from
    - to
    # Required by `DaoPermission.transactionIndex` to mirror the legacy
    # secondary sort key for permission events within a block.
    - transactionIndex

contracts:
  # =============================================
  # Phase 1: Core Registry Contracts (known addresses)
  # =============================================
  - name: DAORegistry
    events:
      - event: DAORegistered(address indexed dao, address indexed creator, string subdomain)

  - name: PluginRepoRegistry
    events:
      - event: PluginRepoRegistered(string subdomain, address pluginRepo)

  - name: PluginSetupProcessor
    events:
      - event: InstallationPrepared(address indexed sender, address indexed dao, bytes32 preparedSetupId, address indexed pluginSetupRepo, (uint8, uint16) versionTag, bytes data, address plugin, (address[], (uint8, address, address, address, bytes32)[]) preparedSetupData)
      - event: InstallationApplied(address indexed dao, address indexed plugin, bytes32 preparedSetupId, bytes32 appliedSetupId)
      - event: UpdatePrepared(address indexed sender, address indexed dao, bytes32 preparedSetupId, address indexed pluginSetupRepo, (uint8, uint16) versionTag, (address, address[], bytes) setupPayload, (address[], (uint8, address, address, address, bytes32)[]) preparedSetupData, bytes initData)
      - event: UpdateApplied(address indexed dao, address indexed plugin, bytes32 preparedSetupId, bytes32 appliedSetupId)
      - event: UninstallationPrepared(address indexed sender, address indexed dao, bytes32 preparedSetupId, address indexed pluginSetupRepo, (uint8, uint16) versionTag, (address, address[], bytes) setupPayload, (uint8, address, address, address, bytes32)[] permissions)
      - event: UninstallationApplied(address indexed dao, address indexed plugin, bytes32 preparedSetupId)

  # =============================================
  # Phase 1: DAO Contract (factory-registered from DAORegistered)
  # =============================================
  - name: DAO
    events:
      - event: MetadataSet(bytes metadata)
      - event: NativeTokenDeposited(address sender, uint256 amount)
      - event: "Granted(bytes32 indexed permissionId, address indexed here, address where, address indexed who, address condition)"
      - event: "Revoked(bytes32 indexed permissionId, address indexed here, address where, address indexed who)"
      # OZ proxy upgrade event — emitted whenever the DAO's implementation
      # contract changes (typically via governance `upgradeToAndCall`).
      # Handler refreshes Dao.implementationAddress + Dao.version.
      - event: Upgraded(address indexed implementation)
      # OSx v1 DAO `Executed` (legacy DAO.abi) — has both `allowFailureMap` and `failureMap`.
      - event: "Executed(address indexed actor, bytes32 callId, (address, uint256, bytes)[] actions, uint256 allowFailureMap, uint256 failureMap, bytes[] execResults)"
      # OSx v2 DAO `Executed` (DaoV2.abi) — same actor/callId/actions/execResults
      # but only `failureMap`. Different signature → different topic hash, must
      # be subscribed separately to catch withdraws on newer DAO contracts.
      - event: "Executed(address indexed actor, bytes32 callId, (address, uint256, bytes)[] actions, uint256 failureMap, bytes[] execResults)"
        name: ExecutedV2

  # =============================================
  # Phase 2: Governance Plugin Contracts (factory-registered)
  # =============================================
  - name: Multisig
    events:
      - event: MultisigSettingsUpdated(bool onlyListed, uint16 indexed minApprovals)
      - event: MembersAdded(address[] members)
      - event: MembersRemoved(address[] members)
      - event: "ProposalCreated(uint256 indexed proposalId, address indexed creator, uint64 startDate, uint64 endDate, bytes metadata, (address, uint256, bytes)[] actions, uint256 allowFailureMap)"
        name: MultisigProposalCreated
      - event: ProposalExecuted(uint256 indexed proposalId)
        name: MultisigProposalExecuted
      - event: Approved(uint256 indexed proposalId, address indexed approver)
      # Inherited from `PluginUUPSUpgradeable` — plugin's own metadata.
      - event: MetadataSet(bytes metadata)
        name: MultisigMetadataSet

  - name: TokenVoting
    events:
      - event: VotingSettingsUpdated(uint8 votingMode, uint32 supportThreshold, uint32 minParticipation, uint64 minDuration, uint256 minProposerVotingPower)
      - event: "ProposalCreated(uint256 indexed proposalId, address indexed creator, uint64 startDate, uint64 endDate, bytes metadata, (address, uint256, bytes)[] actions, uint256 allowFailureMap)"
        name: TokenVotingProposalCreated
      - event: ProposalExecuted(uint256 indexed proposalId)
        name: TokenVotingProposalExecuted
      - event: VoteCast(uint256 indexed proposalId, address indexed voter, uint8 voteOption, uint256 votingPower)
      - event: MetadataSet(bytes metadata)
        name: TokenVotingMetadataSet

  - name: Admin
    events:
      # Admin uses instant execution: ProposalCreated and ProposalExecuted
      # fire in the same tx (the proposal IS the execution). Same struct
      # shape as Multisig/TokenVoting ProposalCreated.
      - event: "ProposalCreated(uint256 indexed proposalId, address indexed creator, uint64 startDate, uint64 endDate, bytes metadata, (address, uint256, bytes)[] actions, uint256 allowFailureMap)"
        name: AdminProposalCreated
      - event: ProposalExecuted(uint256 indexed proposalId)
        name: AdminProposalExecuted
      # Admin holds membership in a separate contract (announced once at install).
      - event: MembershipContractAnnounced(address indexed definingContract)
      - event: MetadataSet(bytes metadata)
        name: AdminMetadataSet

  - name: StagedProposalProcessor
    events:
      - event: ProposalResultReported(uint256 indexed proposalId, uint16 indexed stageId, address indexed body)
      - event: ProposalCanceled(uint256 indexed proposalId, uint16 indexed stageId, address indexed sender)
      - event: "ProposalEdited(uint256 indexed proposalId, uint16 indexed stageId, address indexed sender, bytes metadata, (address, uint256, bytes)[] actions)"
      - event: ProposalAdvanced(uint256 indexed proposalId, uint16 indexed stageId, address indexed sender)
      - event: "ProposalCreated(uint256 indexed proposalId, address indexed creator, uint64 startDate, uint64 endDate, bytes metadata, (address, uint256, bytes)[] actions, uint256 allowFailureMap)"
        name: SPPProposalCreated
      - event: ProposalExecuted(uint256 indexed proposalId)
        name: SPPProposalExecuted
      - event: MetadataSet(bytes metadata)
        name: SPPMetadataSet
      # New stage configuration. The bodies array carries the sub-plugin
      # addresses per stage; the handler links them via PluginParentLink.
      # Inner tuple param names are dropped — Envio's signature parser
      # doesn't accept names on nested tuple components.
      - event: "StagesUpdated(((address,bool,bool,uint8)[],uint64,uint64,uint64,uint16,uint16,bool,bool)[] stages)"
      # Fired when the SPP creates a sub-proposal in a stage body. The
      # handler writes a SubProposalLink so the body's later
      # ProposalCreated can resolve "I am a sub of (parent, stage)".
      - event: SubProposalCreated(uint256 indexed proposalId, uint16 indexed stageId, address indexed body, uint256 bodyProposalId)

  # =============================================
  # Phase 3: Governance Token Contracts (factory-registered)
  # =============================================
  - name: GovernanceERC20
    events:
      - event: DelegateChanged(address indexed delegator, address indexed fromDelegate, address indexed toDelegate)
      - event: DelegateVotesChanged(address indexed delegate, uint256 previousBalance, uint256 newBalance)

  # =============================================
  # Treasury — wildcard ERC-20 Transfer indexing
  # No address list: handler subscribes globally via `wildcard: true` and
  # filters at handler-time on `context.Dao.get(from|to)`. This is Envio's
  # recommended pattern for "indexing transfers for thousands of addresses"
  # (https://docs.envio.dev/docs/HyperIndex/wildcard-indexing).
  # =============================================
  - name: ERC20
    events:
      - event: Transfer(address indexed from, address indexed to, uint256 value)

  # ERC-721 wildcard. Same topic0 as ERC-20 (`Transfer(address,address,uint256)`)
  # but with a third indexed param, so Envio decodes only 4-topic logs here
  # — 3-topic ERC-20 logs go to the ERC20 handler. The two paths are
  # mutually exclusive at the decoder level.
  - name: ERC721
    events:
      - event: Transfer(address indexed from, address indexed to, uint256 indexed tokenId)

  # =============================================
  # Phase 3: VE Governance Contracts (factory-registered via RPC discovery)
  # =============================================
  - name: VotingEscrow
    events:
      - event: Deposit(address indexed depositor, uint256 indexed tokenId, uint256 indexed startTs, uint256 value, uint256 newTotalLocked)
      - event: Withdraw(address indexed depositor, uint256 indexed tokenId, uint256 value, uint256 ts, uint256 newTotalLocked)
      - event: MinDepositSet(uint256 minDeposit)
      - event: TokensDelegated(address indexed sender, address indexed delegatee, uint256[] tokenIds)
      - event: TokensUndelegated(address indexed sender, address indexed delegatee, uint256[] tokenIds)
      # Lock NFT split: token `_from` keeps `_splitAmount1`, new token
      # `newTokenId` is minted with `_splitAmount2`. Mirrors legacy
      # `veGovernance.lockSplit`.
      - event: Split(uint256 indexed _from, uint256 indexed newTokenId, address _sender, uint208 _splitAmount1, uint208 _splitAmount2)
      # Lock NFT merge: `_from` is destroyed, its amount is added to `_to`
      # which now holds `_newTotalAmount`. Mirrors legacy `veGovernance.lockMerge`.
      - event: Merged(address _sender, uint256 indexed _from, uint256 indexed _to, uint208 _fromAmount, uint208 _toAmount, uint208 _newTotalAmount)

  - name: ExitQueue
    events:
      - event: ExitQueued(uint256 indexed tokenId, address indexed holder, uint256 exitDate)
      - event: ExitQueuedV2(uint256 indexed tokenId, address indexed holder, uint48 queuedAt)
      - event: ExitCancelled(uint256 indexed tokenId, address indexed holder)
      - event: MinLockSet(uint48 minLock)
      - event: ExitFeePercentAdjusted(uint256 maxFeePercent, uint256 minFeePercent, uint48 minCooldown, uint8 feeType)

  - name: LockManager
    events:
      - event: BalanceLocked(address indexed voter, uint256 amount)
      - event: BalanceUnlocked(address indexed voter, uint256 amount)

  - name: LockToVote
    events:
      - event: VoteCast(uint256 indexed proposalId, address indexed voter, uint8 voteOption, uint256 votingPower)
        name: LockToVoteVoteCast
      - event: VoteCleared(uint256 indexed proposalId, address indexed voter)
      - event: "ProposalCreated(uint256 indexed proposalId, address indexed creator, uint64 startDate, uint64 endDate, bytes metadata, (address, uint256, bytes)[] actions, uint256 allowFailureMap)"
        name: LockToVoteProposalCreated
      - event: ProposalExecuted(uint256 indexed proposalId)
        name: LockToVoteProposalExecuted
      - event: VotingSettingsUpdated(uint8 votingMode, uint32 supportThresholdRatio, uint32 minParticipationRatio, uint32 minApprovalRatio, uint64 proposalDuration, uint256 minProposerVotingPower)
        name: LockToVoteSettingsUpdated
      - event: MetadataSet(bytes metadata)
        name: LockToVoteMetadataSet

  # =============================================
  # Phase 4: Gauge Voting (factory-registered from gauge plugins)
  # =============================================
  - name: GaugeVoter
    events:
      - event: GaugeCreated(address indexed gauge, address indexed creator, string metadataURI)
      - event: GaugeActivated(address indexed gauge)
      - event: GaugeDeactivated(address indexed gauge)
      - event: GaugeMetadataUpdated(address indexed gauge, string metadataURI)
      - event: Voted(address indexed voter, address indexed gauge, uint256 indexed epoch, uint256 votingPowerCastForGauge, uint256 totalVotingPowerInGauge, uint256 totalVotingPowerInContract, uint256 timestamp)
      - event: Reset(address indexed voter, address indexed gauge, uint256 indexed epoch, uint256 votingPowerRemovedFromGauge, uint256 totalVotingPowerInGauge, uint256 totalVotingPowerInContract, uint256 timestamp)
      - event: MetadataSet(bytes metadata)
        name: GaugeVoterMetadataSet

  # =============================================
  # Phase 4: Capital Distribution (factory-registered from capitalDistributor plugins)
  # =============================================
  - name: CapitalDistributor
    events:
      - event: CampaignCreated(uint256 indexed campaignId, bytes metadataUri, address indexed allocationStrategy, address token, address actionEncoder, uint64 startTime, uint64 endTime)
      - event: CampaignPaused(uint256 indexed campaignId)
      - event: CampaignResumed(uint256 indexed campaignId)
      - event: CampaignEnded(uint256 indexed campaignId)
      # Per-user payout event — drives `CampaignReward.claims[]` and the
      # running `Campaign.claimCount` / `Campaign.totalClaimed` aggregates.
      - event: PayoutClaimed(uint256 indexed campaignId, address indexed recipient, uint256 amount)
      - event: MetadataSet(bytes metadata)
        name: CapitalDistributorMetadataSet

  # MerkleCampaignSet / MerkleCampaignUpdated are emitted by the campaign's
  # ALLOCATION STRATEGY contract (not the CapitalDistributor plugin). The
  # strategy address is dynamically registered from
  # `CapitalDistributor.CampaignCreated` via contractRegister. Mirrors legacy
  # `LogCampaignStrategy.start(allocationStrategy, ...)`.
  - name: CampaignAllocationStrategy
    events:
      - event: MerkleCampaignSet(uint256 indexed campaignId, bytes32 merkleRoot)
      - event: MerkleCampaignUpdated(uint256 indexed campaignId, bytes32 oldMerkleRoot, bytes32 newMerkleRoot)

  # =============================================
  # Phase 4: Execute Selector Condition (factory-registered from condition addresses)
  # =============================================
  - name: ExecuteSelectorCondition
    events:
      - event: SelectorAllowed(bytes4 selector, address where)
      - event: SelectorDisallowed(bytes4 selector, address where)
      - event: NativeTransfersAllowed(address where)
      - event: NativeTransfersDisallowed(address where)

# =============================================
# Chain Configurations
# =============================================
chains:
  # --- Ethereum Mainnet ---
  #
  # HyperSync is the primary event-stream source — the indexer doesn't need
  # an `rpc:` fallback block to sync. Handler-time RPC reads route through
  # `src/helpers/rpcProvider.ts`, which reads `ENVIO_NODES_DRPC_API_KEY` /
  # `ENVIO_NODES_ALCHEMY_API_KEY` / per-chain `ENVIO_RPC_URL_<id>` env vars
  # when present and falls back to public endpoints when they aren't.
  # Keeping env-var interpolation OUT of this file lets `pnpm codegen`
  # succeed in environments that don't expose those keys (CI, Envio's
  # Development plan, fresh local clones).
  - id: 1
    start_block: 16721812
    contracts:
      - name: DAORegistry
        address:
          - 0x7a62da7B56fB3bfCdF70E900787010Bc4c9Ca42e
      - name: PluginRepoRegistry
        address:
          - 0x5B3B36BdC9470963A2734D6a0d2F6a64C21C159f
      - name: PluginSetupProcessor
        address:
          - 0xE978942c691e43f65c1B7c7F8f1dc8cDF061B13f
      - name: DAO
      - name: Multisig
      - name: Admin
      - name: TokenVoting
      - name: StagedProposalProcessor
      - name: GovernanceERC20
      - name: VotingEscrow
      - name: ExitQueue
      - name: LockManager
      - name: LockToVote
      - name: GaugeVoter
      - name: CapitalDistributor
      - name: CampaignAllocationStrategy
      - name: ExecuteSelectorCondition
      - name: ERC20
      - name: ERC721

#   # --- Ethereum Sepolia ---
#   - id: 11155111
#     start_block: 4415294
#     rpc:
#       - url: https://lb.drpc.org/ogrpc?network=sepolia&dkey=${ENVIO_NODES_DRPC_API_KEY}
#       - url: https://eth-sepolia.g.alchemy.com/v2/${ENVIO_NODES_ALCHEMY_API_KEY}
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0x308a1DC5020c4B5d992F5543a7236c465997fecB
#       - name: PluginRepoRegistry
#         address:
#           - 0x35B62715459cB60bf6dC17fF8cfe138EA305E7Ee
#       - name: PluginSetupProcessor
#         address:
#           - 0xC24188a73dc09aA7C721f96Ad8857B469C01dC9f
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Polygon Mainnet ---
#   - id: 137
#     start_block: 40828385
#     rpc:
#       - url: https://lb.drpc.org/ogrpc?network=polygon&dkey=${ENVIO_NODES_DRPC_API_KEY}
#       - url: https://polygon-mainnet.g.alchemy.com/v2/${ENVIO_NODES_ALCHEMY_API_KEY}
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0x96E54098317631641703404C06A5afAD89da7373
#       - name: PluginRepoRegistry
#         address:
#           - 0xA03C2182af8eC460D498108C92E8638a580b94d4
#       - name: PluginSetupProcessor
#         address:
#           - 0x879D9dfe3F36d7684BeC1a2bB4Aa8E8871A7245B
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Arbitrum Mainnet ---
#   - id: 42161
#     start_block: 145461945
#     rpc:
#       - url: https://lb.drpc.org/ogrpc?network=arbitrum&dkey=${ENVIO_NODES_DRPC_API_KEY}
#       - url: https://arb-mainnet.g.alchemy.com/v2/${ENVIO_NODES_ALCHEMY_API_KEY}
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0xB5146Fd572C669ABC353902e43F47fda4609E38A
#       - name: PluginRepoRegistry
#         address:
#           - 0xCe0B4124dea6105bfB85fB4461c4D39f360E9ef3
#       - name: PluginSetupProcessor
#         address:
#           - 0x308a1DC5020c4B5d992F5543a7236c465997fecB
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Base Mainnet ---
#   - id: 8453
#     start_block: 2094661
#     rpc:
#       - url: https://lb.drpc.org/ogrpc?network=base&dkey=${ENVIO_NODES_DRPC_API_KEY}
#       - url: https://base-mainnet.g.alchemy.com/v2/${ENVIO_NODES_ALCHEMY_API_KEY}
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0xeB98a71d69a1e12B62c10368D9dA5364CE0f7178
#       - name: PluginRepoRegistry
#         address:
#           - 0xB5eB5C011827C9F5787ceE3Abc72d247E36a5a0D
#       - name: PluginSetupProcessor
#         address:
#           - 0x91a851E9Ed7F2c6d41b15F76e4a88f5A37067cC9
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- zkSync Mainnet ---
#   - id: 324
#     start_block: 37460684
#     rpc:
#       - url: https://lb.drpc.org/ogrpc?network=zksync&dkey=${ENVIO_NODES_DRPC_API_KEY}
#       - url: https://zksync-mainnet.g.alchemy.com/v2/${ENVIO_NODES_ALCHEMY_API_KEY}
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0xE7351bA0DDCc52249F27353893BcDBC74229e99d
#       - name: PluginRepoRegistry
#         address:
#           - 0xEa26fC4028D9293f453804b40F097F11974FdB79
#       - name: PluginSetupProcessor
#         address:
#           - 0x8E3e98ECF5CdBF2bEcCD91d3BA580D472df5A0cB
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Optimism Mainnet ---
#   - id: 10
#     start_block: 135600978
#     rpc:
#       - url: https://lb.drpc.org/ogrpc?network=optimism&dkey=${ENVIO_NODES_DRPC_API_KEY}
#       - url: https://opt-mainnet.g.alchemy.com/v2/${ENVIO_NODES_ALCHEMY_API_KEY}
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0xDAb3AB22cdd41ab299d7D674714Ca94e9B63F9dD
#       - name: PluginRepoRegistry
#         address:
#           - 0x22A80a41893Cf0211b345de12825800D21C73102
#       - name: PluginSetupProcessor
#         address:
#           - 0x2379Dc18B4A939a2B76F5c79f58aA49193DA56C2
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Avalanche Mainnet ---
#   - id: 43114
#     start_block: 63340971
#     rpc:
#       - url: https://lb.drpc.org/ogrpc?network=avalanche&dkey=${ENVIO_NODES_DRPC_API_KEY}
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0xe076b37971b80689540426d7B8c739338aA21Bac
#       - name: PluginRepoRegistry
#         address:
#           - 0x52Df25812774119d22467bDfDeF45D66f4061e7C
#       - name: PluginSetupProcessor
#         address:
#           - 0x95902780BD390A32f2E1EDf02623F8e636CEE518
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Katana Mainnet ---
#   - id: 747474
#     start_block: 15070184
#     rpc:
#       - url: https://disabled-chain-katana
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0x44e3b4e36276735aAf8C01D54D5BBd93f8d202ae
#       - name: PluginRepoRegistry
#         address:
#           - 0x344347fF8CB8E3A68bACc39Ffd4C236e2b0C94dA
#       - name: PluginSetupProcessor
#         address:
#           - 0x6240E3afA085B8393Eb072911f3D65EF080B6bEf
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Peaq Mainnet ---
#   - id: 3338
#     start_block: 4032399
#     rpc:
#       - url: https://disabled-chain-peaq
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0x95D563382BeD5AcB458759EE05b27DF2CB019Cc7
#       - name: PluginRepoRegistry
#         address:
#           - 0xE640Da5AD169630555A86D9b6b9C145B4961b1EB
#       - name: PluginSetupProcessor
#         address:
#           - 0x08633901DdF9cD8e2DC3a073594d0A7DaD6f3f57
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Chiliz Mainnet ---
#   - id: 88888
#     start_block: 23759200
#     rpc:
#       - url: https://disabled-chain-chiliz
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0xa81Ebd1165cBE602f2BB28C956b180202f7049Ef
#       - name: PluginRepoRegistry
#         address:
#           - 0x16eEbBD27860F7E3c5413fcC560dA8D048944ae0
#       - name: PluginSetupProcessor
#         address:
#           - 0xD39fd78987000C1aa96209D76Bec576F31Dbc9BE
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
# 
#   # --- Citrea Mainnet ---
#   - id: 4114
#     start_block: 5471881
#     rpc:
#       - url: https://disabled-chain-citrea
#     contracts:
#       - name: DAORegistry
#         address:
#           - 0x0e30F6A8119E2528511C7B2d034d89dAb1cEC6b8
#       - name: PluginRepoRegistry
#         address:
#           - 0xAf22BE414A2339B5483e47e8C33f4e7b4aDb9380
#       - name: PluginSetupProcessor
#         address:
#           - 0xc3a857B614D5B79B936Ce6640E5351dF8fA262eD
#       - name: DAO
#       - name: Multisig
#       - name: TokenVoting
#       - name: StagedProposalProcessor
#       - name: GovernanceERC20
#       - name: VotingEscrow
#       - name: ExitQueue
#       - name: LockManager
#       - name: LockToVote
#       - name: GaugeVoter
#       - name: CapitalDistributor
#       - name: ExecuteSelectorCondition
