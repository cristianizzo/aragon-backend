# Dependencies
node_modules/
.pnpm-store

# Generated (regenerated via pnpm codegen)
generated/

# Environment
.env
.env.local

# Build
build/
dist/

# IDE
.vscode/
.idea/

# Rescript artifacts
*.exe
*.obj
*.out
*.compile
*.native
*.byte
*.cmo
*.annot
*.cmi
*.cmx
*.cmt
*.cmti
*.cma
*.a
*.cmxa
*~
*.cmj
*.bak
lib/*
*.mlast
*.mliast
.merlin
.bsb.lock
*.res.js
*.res.mjs

# Envio
cache
!.envio/cache/
artifacts
benchmarks/

# Migration reference (not part of the indexer)
migration/

# Local-only scripts (diagnostic, deployment, table tracking — not part of
# the indexer source). Each developer can keep their own; not for the PR.
scripts/

# Internal docs + agent harness — kept locally for AI-assisted workflows
# but excluded from the upstream repo to avoid polluting it with
# Claude/Codex specific scaffolding.
docs/
AGENTS.md
CLAUDE.md
MIGRATION_STATUS.md
.claude/

# OS
.DS_Store
Thumbs.db

# Lock files (keep pnpm-lock.yaml)
package-lock.json
